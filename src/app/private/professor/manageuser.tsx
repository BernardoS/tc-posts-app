import { Text } from "react-native"
// Vai listar os dois tipos de usuários e apontar para a rota de list user
export default function ManageUser(){
    return (
        <Text>Manage User</Text>
    )
}